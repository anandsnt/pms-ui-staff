var CheckoutSuccessModal = function(callBack) {
	BaseModal.call(this);
	var that = this;
	this.url = "ui/roomAssignmentModal";

	this.delegateEvents = function() {
		// that.myDom.find('.message').html(message);
		// that.myDom.find('#modal-close,#ok').on('click',that.okButtonClicked);
	};
	
	
};