sntRover.controller('appController',['$scope', '$state', function($scope, $state){

	$scope.init = function () {
		// preselect the current reservation group
		alert("main");
		
    }

    $scope.init();
}]);


sntRover.controller('mainNavController',['$scope', '$state', function($scope, $state){

	$scope.init = function () {
		alert("mainNavController");
		// preselect the current reservation group
		
    }

    $scope.init();
}]);
